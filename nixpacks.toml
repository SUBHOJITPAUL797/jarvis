[phases.setup]
nixPkgs = ["python39", "opencv", "ffmpeg", "pip"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python app.py"
